[00:00] Welcome everyone to today’s lecture on Machine Learning basics. Machine learning is teaching computers to learn from data instead of giving them explicit rules. Let’s quickly go over supervised learning. The system is trained on labeled data. An example is an email marked as spam or not spam. The model learns patterns and then predicts unseen cases. Common algorithms include decision trees, support vector machines, and neural networks.

[01:00] Unsupervised learning is when the system finds patterns without labels. For instance, clustering customers into groups based on their purchasing behavior. K-means is a classic example. Reinforcement learning is another type. An agent interacts with an environment and learns from feedback or rewards. A famous example is training an AI to play video games or control a robot. Challenges in machine learning include overfitting, where the model memorizes training data but performs poorly on new data, and bias, which happens when the data itself is not balanced. Applications: machine learning is used in healthcare for disease prediction, in finance for fraud detection, and in self-driving cars for object recognition. That’s all for today’s introduction. In the next lecture, we will cover evaluation metrics: accuracy, precision, recall, and F1-score.